var buff1 : GameObject;
var buff2 : GameObject;


// Use this for initialization
function Start () {
}

// Update is called once per frame
function Update () {
	buff1.transform.Rotate(0,0,2.8);
	buff2.transform.Rotate(0,0,2.8);
}

